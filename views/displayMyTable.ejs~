<!doctype html>
<html>
  <head>
<!doctype html>
<html>
  <head>
      <link rel="stylesheet" type="text/css" href="/stylesheet.css">
</head>
<% include header %>

<% include menu %>

 <div id="content" style="background-color:#848484;height:630px;width:600px;float:left;text-ali\
gn:left">
 <h3> Major Superhero Groups in the US</h3>

   <% if(rs.length > 0) { %>
    <table border="1" style=width"300px">

        <tr><th>Hero</th><th>Group Name</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
            <tr>
             <td> <%= rs[i].Hero %></td>
             <td>  <%= rs[i].Group %></td>
            </tr>
        <% } %>
    </table>
    <% } else { %>
         No users are entered into the database.
    <% } %>
</div>
<% include footer %>
